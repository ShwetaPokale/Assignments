Hello,

All the three components are added with respeactive farmeworks as asked in the mail. The recording is also uploaded.
Also Databse creation query is been shared below.

Note: If you want to run the components please use your credentials.




CREATE TABLE users (
    user_id INT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(50) NOT NULL,
    email VARCHAR(100) NOT NULL,
    password VARCHAR(255) NOT NULL
);

